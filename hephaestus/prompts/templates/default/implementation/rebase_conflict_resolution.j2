Rebase Conflict Resolution

{{ untrusted_notice }}

The host paused a signed-policy rebase. This is a narrow edit-only task.
Resolve the current conflicts in the listed paths.

Allowed paths:
{{ conflict_paths_block }}

{% if diagnosis_block %}Host diagnosis from the previous turn:
{{ diagnosis_block }}

{% endif %}Current bounded conflict hunks:
{{ conflict_hunks_block }}

Rules:
- Edit only the allowed paths.
- Remove all conflict markers from each allowed path.
- Preserve the compatible behavior from both sides of each hunk.
- Do not run Git commands, stage files, continue or abort the rebase, commit, or push.
- Do not edit any other path or perform full implementation work.

After the edits, return a concise summary of the content changes. If the safe resolution is
unclear, leave the files unchanged and explain the blocker.
